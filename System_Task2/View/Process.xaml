<Window x:Class="System_Task2.View.Process"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:System_Task2.View"
        mc:Ignorable="d"
        Title="Process" Height="250" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="7*"/>
        </Grid.RowDefinitions>
        <WrapPanel>
        <Button Width="200" Height="30">Process</Button>
        <Button Width="200"  Height="30">Log File</Button>
        <Button Width="190" Height="30" >Configuration</Button>
        </WrapPanel>

        
        <ListView ItemsSource="{Binding ProcessList}" Grid.Row="1" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name"  Width="400">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                                <TextBlock Text="{Binding Title}" TextDecorations="Underline" Foreground="Blue" Cursor="Hand" />
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
